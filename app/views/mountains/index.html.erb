<div class="row">
  <div class="col-md-4">
    <ul class="nav nav-pills nav-stacked">
      <li><a href="#">Checklist (Checkboxes for lists?)</a></li>
      <li><a href="#">State (checkboxes)</a></li>
      <li><a href="#">Height (4000-foot-plus, 3K-4K, less than 3K - checkboxes)</a></li>
      <li><a href="#">Hiked/Not Hiked (Checkboxes)</a></li>
    </ul>
  </div>
  <div class="col-md-8">
    <div class="panel panel-default">
    <!-- Default panel contents -->
      <div class="panel-heading">Mountains</div>

        <table class="table">
          <tr>
            <th><%= sortable "name" %></th>
            <th><%= sortable "height" %></th>
          </tr>
          <% @mountains.each do |mountain| %>   
            <tr>
              <td><%= link_to "#{mountain.name}", mountain %></td>
              <td><%= mountain.height %></td>
            </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
</div>
